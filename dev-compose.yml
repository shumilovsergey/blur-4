services:
  blur:
    build:
      context: .
      target: dev
    ports:
      - "${PORT:-8891}:${PORT:-8891}"
    env_file: .env
    volumes:
      - ./build:/app
      - go-cache:/root/go/pkg/mod

volumes:
  go-cache:
